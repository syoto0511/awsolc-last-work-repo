version: 0.2

phases:
  build:
    commands:
      - aws deploy push --application-name awsolc-last-work-codedeploy --s3-location s3://awsolc-last-work-s3/artifact.zip --source src
artifacts:
  files:
    - '**/*'
  base-directory: src